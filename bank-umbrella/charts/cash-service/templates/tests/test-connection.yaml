apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cash-service.fullname" . }}-test-connection"
  labels:
    {{- include "cash-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox:latest
      command:
        - wget
        - --spider
        - --timeout=30
        - "http://{{ include "cash-service.fullname" . }}:8082/actuator/health"
  restartPolicy: Never
