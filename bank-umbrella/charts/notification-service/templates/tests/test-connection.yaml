apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "notification-service.fullname" . }}-test-connection"
  labels:
    {{- include "notification-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox:latest
      command:
        - wget
        - --spider
        - --timeout=30
        - "http://{{ include "notification-service.fullname" . }}:8086/actuator/health"
  restartPolicy: Never
