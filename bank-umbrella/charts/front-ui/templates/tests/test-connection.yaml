apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "front-ui.fullname" . }}-test-connection"
  labels:
    {{- include "front-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox:latest
      command:
        - wget
        - --spider
        - --timeout=30
        - "http://{{ include "front-ui.fullname" . }}:8088/actuator/health"
  restartPolicy: Never
